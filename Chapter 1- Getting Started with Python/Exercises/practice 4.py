# find area of a triangle

base = float(input("Enter the base of a triangle: "))
height = float(input("Enter the height of a triangle: "))

# compute 
area = (base * height) / 2

# display 
print ("The area of a triangle using", base, "and", height)
